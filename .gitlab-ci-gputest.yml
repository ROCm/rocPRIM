test:vega20:
  extends: .test
  needs:
    - build:cmake-latest
  tags:
    - vega20
    - rocm

test:s9300:
  extends: .test
  needs:
    - build:cmake-latest
  tags:
    - s9300
    - rocm
  allow_failure: true

test:mi25:
  extends: .test
  needs:
    - build:cmake-latest
  tags:
    - mi25
    - rocm 
